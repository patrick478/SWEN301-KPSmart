<Page x:Class="Client.RoutesPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      mc:Ignorable="d" 
      d:DesignHeight="300" d:DesignWidth="300"
	  Title="KPSmart - Routes">

    <Grid>

        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition Height="Auto"/>
            <RowDefinition />
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- People list -->
        <Border Grid.Column="0" Grid.Row="0" Height="35" Padding="5" Background="#4E87D4">
            <Label VerticalAlignment="Center" Foreground="White">Names</Label>
        </Border>
        <ListBox Name="routesListBox" Grid.Column="0" Grid.Row="1">
            <ListBoxItem>Wellington->Christchurch</ListBoxItem>
            <ListBoxItem>Christchurch->Wellington</ListBoxItem>
            <ListBoxItem>Wellington->Melbourne</ListBoxItem>
            <ListBoxItem>Auckland->Wellington</ListBoxItem>
        </ListBox>
    </Grid>
</Page>
